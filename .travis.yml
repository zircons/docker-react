language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t rpellakuru/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rpellakuru/docker-react npm run test -- --coverage